# Tutorial: community module

Create and validate the module, test install order in a clean site, include GPL-3.0-or-later source
and dependency notices, and build an artifact. Remote marketplace publication is unavailable in v1;
distribution is an explicit maintainer action outside the CLI.
