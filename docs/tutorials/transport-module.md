# Tutorial: transport module

Generate with `noxusai module create dispatch`, compare the result with bundled `noxus_transport`,
then validate it. Use manifest dependencies for optional maintenance, permission-aware APIs for trip
dispatch, and the location event endpoint for validated latitude/longitude updates.
